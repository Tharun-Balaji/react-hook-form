
export const patterns = {
  email: /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/,
};

/**
 * The base URL of the REST API.
 * This is used to construct the full URL of each query.
 */
export const url = "https://react-hook-form-backend.onrender.com";